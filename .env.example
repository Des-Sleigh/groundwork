# Copy to .env (gitignored). Read by the Anthropic SDK via the environment.
ANTHROPIC_API_KEY=

# Optional: point offline search/fetch at a different corpus directory.
# Defaults to the repo's corpus/ + redteam/ fixtures.
# GROUNDWORK_CORPUS=/path/to/corpus
